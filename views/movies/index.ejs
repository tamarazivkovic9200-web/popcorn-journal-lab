<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Watchlist | Popcorn Journal</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body class="landing-page watchlist-page">
  <div class="landing-card">
    <h1 class="app-title">🎬 Your Watchlist</h1>

    <!-- 🧭 NAV BUTTONS -->
    <div class="auth-buttons">
      <a href="/movies/new" class="btn add-movie">Add New Movie</a>
      <a href="/" class="btn home">Home</a>
    </div>

    <!-- 🎞️ TYPE FILTERS -->
    <div class="filter-grid">
      <a href="/movies" class="<%= !type ? 'active' : '' %>">All</a>
      <a href="/movies?type=Movie" class="<%= type === 'Movie' ? 'active' : '' %>">Movies 🎬</a>
      <a href="/movies?type=TV%20Show" class="<%= type === 'TV Show' ? 'active' : '' %>">TV Shows 📺</a>
    </div>

    <!-- ✅ WATCHED FILTERS — show only if a type is selected -->

    <% if (type) { %>
      <div class="filter-grid new-filters ">
        <a href="/movies?type=<%= type %>" class="<%= !filter ? 'active' : '' %>">
          All
        </a>
        <a href="/movies?type=<%= type %>&filter=Watched"
           class="<%= filter === 'Watched' ? 'active' : '' %>">
          Watched 
        </a>
        <a href="/movies?type=<%= type %>&filter=ToWatch"
           class="<%= filter === 'ToWatch' ? 'active' : '' %>">
          To Watch 
        </a>
      </div>
    <% } %>

    <% if (movies.length === 0) { %>
      <p class="tagline">No movies yet — start building your collection!</p>
    <% } else { %>
      <div class="movie-grid">
        <% movies.forEach(movie => { %>
          <div class="movie-card <%= movie.watched ? 'watched' : 'unwatched' %>">
            
            <!-- 🎞 POSTER -->
            <img 
              src="<%= movie.image %>" 
              alt="<%= movie.title %> poster" 
              class="movie-poster"
              onerror="this.src='/images/placeholder.png'"
            />

            <!-- 🎬 INFO -->
            <div class="movie-header">
              <strong class="movie-title"><%= movie.title %></strong>
              <span class="movie-type">(<%= movie.type %>, <%= movie.releaseYear %>)</span>
            </div>

            <% if (movie.genre && movie.genre.length > 0) { %>
              <p class="movie-genre"><%= movie.genre.join(', ') %></p>
            <% } %>

            <p class="movie-status">
              <% if (movie.watched) { %> ✅ Watched <% } else { %> ⏳ To Watch <% } %>
            </p>

            <!-- ⚙️ ACTIONS -->
            <div class="movie-actions">
              <a href="/movies/<%= movie._id %>/edit" class="btn small sign-in">✏️ Edit</a>
              <a href="/movies/<%= movie._id %>/delete" class="btn small sign-up">🗑 Delete</a>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>